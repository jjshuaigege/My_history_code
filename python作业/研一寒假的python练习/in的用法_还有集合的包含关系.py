#in用来判断某个元素是否在列表、集合里面（只能判断单个元素，不能判断多个元素！）
#用于字符串时可判断某个子字符串是否在原字符串（可判断多个元素）
a=[5,6]
b=[1,[5,6],8]
c=[1,5,6,8]
#<= y用于集合之间，可判断集合是否为包含关系
a1={5,6}
c1={1,5,6,8}
print("a in b:",a in b)
print("a in c:",a in c)
print("a1 <= c1:",a1 <= c1)